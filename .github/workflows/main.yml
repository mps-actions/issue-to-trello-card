on:
  issues:
    types: [opened]

env:
  SAMPLE_SECRET: ${{secrets.SAMPLE_SECRET}}

jobs:
  create-card:
    runs-on: ubuntu-latest
    name: A job to create card.
    steps:
    - name: Echo sample secret.
      id: echo-sample-secret
      uses: mps-actions/issue-to-trello-card@main
